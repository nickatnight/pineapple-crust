version: '2'
services:
    app:
        container_name: app
        build:
            context: ./app
            dockerfile: ./Dockerfile
        env_file:
            - .env
        ports:
            - "5000:5000"

    nginx:
        container_name: nginx
        build:
            context: ./nginx
            dockerfile: ./Dockerfile
        depends_on:
            - app
        ports:
            - "9080:80"
